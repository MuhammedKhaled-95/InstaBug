class Movie{

    constructor(overview, title,poster,date) {
        this.overview = overview;
        this.title = title;
        this.poster = poster;
        this.date = date;
      }
}

export default Movie;


// A Movie class is created to store the movies content in a structured format